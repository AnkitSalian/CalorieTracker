const express = require('express');

const router = express.Router();

// const { protect } = require('../middleware/auth');

// router.post('/calorie/add', protect, addFood);

// router.patch('/calorie/:id', protect, updateFood);

// router.delete('/calorie/:id', protect, deleteFood);

//router.get('/calorie', protect, fetchFood );

module.exports = router;